import WorkflowApp from "./WorkflowApp";

export const metadata = {
  title: "Agent Workflows | Azure Durable Functions Gated Pipeline",
};

export default function AgentWorkflowsPage() {
  return <WorkflowApp />;
}
